// @ts-nocheck — build artifact; types are checked at source (packages/core-inheritance)
// AUTO-GENERATED by scripts/build-core-bundle.mjs from ../../packages/core-inheritance/src/index.js
// DO NOT EDIT MANUALLY. Run `npm run build:core` to regenerate.
// Source of truth: /Users/chouchunyeh/Desktop/choulegal/packages/core-inheritance/

// ../../packages/core-inheritance/src/estate-tax.js
var V2025 = {
  lawVersion: "2025 (\u8CA1\u653F\u90E8 113/11/28 \u516C\u544A)",
  appliesFrom: "2025-01-01",
  exemption: 1333e4,
  // 免稅額 1,333 萬（§18，113/11/28 公告）
  deductSpouse: 553e4,
  // 配偶扣除 553 萬（§17-I-1，113/11/28 公告）
  deductFuneral: 138e4,
  // 喪葬費扣除 138 萬（§17-I-10，113/11/28 公告）
  deductPerChild: 56e4,
  // 直系血親卑親屬每人 56 萬（§17-I-2，113/11/28 公告）
  deductPerParent: 138e4,
  // 父母每人 138 萬（§17-I-3，113/11/28 公告）
  deductPerDisabled: 693e4,
  // 身心障礙每人 693 萬（§17-I-4，113/11/28 公告）
  // ⚠️ 重要：insuranceExcludeMax 並非遺贈稅法明文規定的「不計入遺產總額」上限。
  //   遺贈稅§16-I-9 只規定「約定於本人死亡時給付其指定受益人之人壽保險金額」不計入遺產，
  //   本條文並無金額上限；是否計入遺產改以「實質課稅原則」個案判斷
  //   （司法院大法官釋字第 420 號、財政部 101.7.13 台財稅字第 10100107900 號函）。
  //   本欄位為「所得基本稅額條例§12-I-2 保險死亡給付免稅額」（最低稅負制 AMT），
  //   借用作為「大額保險金可能觸發 實質課稅原則」的警示門檻，非遺產稅法之嚴格規定。
  //   114 年度該免稅額自 3,330 萬調升至 3,740 萬（財政部 113/11/28 公告）。
  insuranceExcludeMax: 374e5,
  // 所得基本稅額條例§12-I-2 保險死亡給付免稅額（AMT 概念，非遺贈稅上限）
  brackets: [
    { upTo: 5621e4, rate: 0.1 },
    // 5,621 萬以下 10%
    { upTo: 11242e4, rate: 0.15 },
    // 5,621 萬 ~ 1億1,242萬 15%
    { upTo: Infinity, rate: 0.2 }
    // 超過 1億1,242萬 20%
  ]
};
var V2022_2023 = {
  lawVersion: "2022-2023 (\u8CA1\u653F\u90E8 110/11/24 \u516C\u544A)",
  appliesFrom: "2022-01-01",
  exemption: 1333e4,
  // 免稅額 1,333 萬
  deductSpouse: 493e4,
  // 配偶扣除 493 萬
  deductFuneral: 123e4,
  // 喪葬費扣除 123 萬
  deductPerChild: 5e5,
  // 直系血親卑親屬每人 50 萬
  deductPerParent: 123e4,
  // 父母每人 123 萬
  deductPerDisabled: 618e4,
  // 身心障礙每人 618 萬
  insuranceExcludeMax: 333e5,
  // 見 V2025 同名欄位說明：此為 AMT 借用門檻，非遺贈稅硬性上限；2022-2023 年 AMT 免稅額為 3,330 萬
  brackets: [
    { upTo: 5e7, rate: 0.1 },
    // 5,000 萬以下 10%
    { upTo: 1e8, rate: 0.15 },
    // 5,000 萬 ~ 1 億 15%
    { upTo: Infinity, rate: 0.2 }
    // 超過 1 億 20%
  ]
};
var V2024 = {
  ...V2022_2023,
  lawVersion: "2024 (\u907A\u8D08\u7A05\u6CBF\u7528 110/11/24 \u516C\u544A\uFF1BAMT \u9580\u6ABB\u6539\u7528 112/11/23 \u516C\u544A 3,740 \u842C)",
  appliesFrom: "2024-01-01",
  insuranceExcludeMax: 374e5
  // 113 年度 AMT 免稅額（財政部 112/11/23 公告）
};
var ESTATE_TAX_VERSIONS = {
  /** 2022 版：財政部 110/11/24 公告，自 111/01/01 起適用 */
  2022: V2022_2023,
  /** 2023 版：CPI 未達 10% 門檻，沿用 2022 版 */
  2023: V2022_2023,
  /** 2024 版：遺贈稅沿用，但 AMT 保險給付免稅額於 113 年度調升至 3,740 萬 */
  2024: V2024,
  /** 2025 版：財政部 113/11/28 公告，自 114/01/01 起適用 */
  2025: V2025,
  /** 2026 版：CPI 未達 10% 門檻，沿用 2025 版 */
  2026: V2025
};
var LATEST_TAX_YEAR = 2026;
var ESTATE_EXEMPTION = ESTATE_TAX_VERSIONS[LATEST_TAX_YEAR].exemption;
var DEDUCT_SPOUSE = ESTATE_TAX_VERSIONS[LATEST_TAX_YEAR].deductSpouse;
var DEDUCT_FUNERAL = ESTATE_TAX_VERSIONS[LATEST_TAX_YEAR].deductFuneral;
var DEDUCT_PER_CHILD = ESTATE_TAX_VERSIONS[LATEST_TAX_YEAR].deductPerChild;
var DEDUCT_PER_PARENT = ESTATE_TAX_VERSIONS[LATEST_TAX_YEAR].deductPerParent;
var DEDUCT_PER_DISABLED = ESTATE_TAX_VERSIONS[LATEST_TAX_YEAR].deductPerDisabled;
var INSURANCE_EXCLUDE_MAX = ESTATE_TAX_VERSIONS[LATEST_TAX_YEAR].insuranceExcludeMax;
function getVersion(year) {
  const y = year || LATEST_TAX_YEAR;
  return ESTATE_TAX_VERSIONS[
    /** @type {keyof typeof ESTATE_TAX_VERSIONS} */
    y
  ] || ESTATE_TAX_VERSIONS[LATEST_TAX_YEAR];
}
function calcEstateTax(netEstate, opts = {}) {
  if (netEstate <= 0) return 0;
  const { brackets } = getVersion(opts.year);
  let remaining = netEstate;
  let prevUpTo = 0;
  let tax = 0;
  for (const b of brackets) {
    const slice = Math.min(remaining, b.upTo - prevUpTo);
    tax += slice * b.rate;
    remaining -= slice;
    prevUpTo = b.upTo;
    if (remaining <= 0) break;
  }
  return Math.round(tax);
}
function getEstateTaxRate(netEstate, opts = {}) {
  const { brackets } = getVersion(opts.year);
  let prevUpTo = 0;
  for (const b of brackets) {
    if (netEstate <= b.upTo) return b.rate * 100 + "%";
    prevUpTo = b.upTo;
  }
  return brackets[brackets.length - 1].rate * 100 + "%";
}
function getEstateTaxBreakdown(netEstate, opts = {}) {
  if (netEstate <= 0) return [];
  const { brackets } = getVersion(opts.year);
  const out = [];
  let remaining = netEstate;
  let prevUpTo = 0;
  for (const b of brackets) {
    if (remaining <= 0) break;
    const slice = Math.min(remaining, b.upTo - prevUpTo);
    out.push({ rate: b.rate, base: slice, tax: Math.round(slice * b.rate) });
    remaining -= slice;
    prevUpTo = b.upTo;
  }
  return out;
}
function calcEstateTaxFull(p) {
  const {
    totalAssets,
    insurancePaid = 0,
    hasSpouse = false,
    childrenCount = 0,
    minorChildYears = 0,
    parentsCount = 0,
    disabledCount = 0,
    otherDeductions = 0,
    agriDeduction = 0,
    retaxDeduction = 0,
    year
  } = p;
  const v = getVersion(year);
  const insuranceTaxable = Math.max(0, insurancePaid - v.insuranceExcludeMax);
  const insuranceExcluded = Math.min(insurancePaid, v.insuranceExcludeMax);
  const adjustedTotal = totalAssets + insuranceTaxable;
  const dSpouse = hasSpouse ? v.deductSpouse : 0;
  const dFuneral = v.deductFuneral;
  const dChildren = Math.max(0, childrenCount) * v.deductPerChild;
  const dMinorChildren = Math.max(0, minorChildYears) * v.deductPerChild;
  const dParents = Math.max(0, parentsCount) * v.deductPerParent;
  const dDisabled = Math.max(0, disabledCount) * v.deductPerDisabled;
  const totalDeductions = dSpouse + dFuneral + dChildren + dMinorChildren + dParents + dDisabled + otherDeductions + agriDeduction + retaxDeduction;
  const netEstate = Math.max(0, adjustedTotal - v.exemption - totalDeductions);
  const tax = calcEstateTax(netEstate, { year });
  return {
    adjustedTotal,
    insuranceExcluded,
    insuranceTaxable,
    deductions: {
      spouse: dSpouse,
      funeral: dFuneral,
      children: dChildren,
      minorChildren: dMinorChildren,
      parents: dParents,
      disabled: dDisabled,
      other: otherDeductions,
      agriculture: agriDeduction,
      reinherit: retaxDeduction,
      total: totalDeductions
    },
    exemption: v.exemption,
    netEstate,
    tax,
    rate: getEstateTaxRate(netEstate, { year }),
    breakdown: getEstateTaxBreakdown(netEstate, { year }),
    taxYear: year || LATEST_TAX_YEAR,
    lawVersion: v.lawVersion,
    law: "\u907A\u7522\u53CA\u8D08\u8207\u7A05\u6CD5\xA713, \xA717, \xA716"
  };
}

// ../../packages/core-inheritance/src/gift-tax.js
var V20252 = {
  lawVersion: "2025 (\u8CA1\u653F\u90E8 113/11/28 \u516C\u544A)",
  appliesFrom: "2025-01-01",
  annualExempt: 244e4,
  // 年度免稅額 244 萬（§22）
  brackets: [
    { upTo: 2811e4, rate: 0.1 },
    // 2,811 萬以下 10%
    { upTo: 5621e4, rate: 0.15 },
    // 2,811 萬 ~ 5,621 萬 15%
    { upTo: Infinity, rate: 0.2 }
    // 超過 5,621 萬 20%
  ]
};
var V2022_2024 = {
  lawVersion: "2022-2024 (\u8CA1\u653F\u90E8 110/11/24 \u516C\u544A)",
  appliesFrom: "2022-01-01",
  annualExempt: 244e4,
  // 年度免稅額 244 萬（§22；111 年自 220 萬調升）
  brackets: [
    { upTo: 25e6, rate: 0.1 },
    // 2,500 萬以下 10%
    { upTo: 5e7, rate: 0.15 },
    // 2,500 萬 ~ 5,000 萬 15%
    { upTo: Infinity, rate: 0.2 }
    // 超過 5,000 萬 20%
  ]
};
var GIFT_TAX_VERSIONS = {
  /** 2022 版：財政部 110/11/24 公告，自 111/01/01 起適用 */
  2022: V2022_2024,
  /** 2023 版：CPI 未達 10% 門檻，沿用 2022 版 */
  2023: V2022_2024,
  /** 2024 版：CPI 未達 10% 門檻，沿用 2022 版 */
  2024: V2022_2024,
  /** 2025 版：財政部 113/11/28 公告，自 114/01/01 起適用 */
  2025: V20252,
  /** 2026 版：CPI 未達 10% 門檻，沿用 2025 版 */
  2026: V20252
};
var LATEST_GIFT_YEAR = 2026;
var GIFT_ANNUAL_EXEMPT = GIFT_TAX_VERSIONS[LATEST_GIFT_YEAR].annualExempt;
function getVersion2(year) {
  return GIFT_TAX_VERSIONS[
    /** @type {keyof typeof GIFT_TAX_VERSIONS} */
    year || LATEST_GIFT_YEAR
  ] || GIFT_TAX_VERSIONS[LATEST_GIFT_YEAR];
}
function calcGiftTax(netGift, opts = {}) {
  if (netGift <= 0) return 0;
  const { brackets } = getVersion2(opts.year);
  let remaining = netGift;
  let prevUpTo = 0;
  let tax = 0;
  for (const b of brackets) {
    const slice = Math.min(remaining, b.upTo - prevUpTo);
    tax += slice * b.rate;
    remaining -= slice;
    prevUpTo = b.upTo;
    if (remaining <= 0) break;
  }
  return Math.round(tax);
}
function getGiftTaxRate(netGift, opts = {}) {
  const { brackets } = getVersion2(opts.year);
  for (const b of brackets) {
    if (netGift <= b.upTo) return b.rate * 100 + "%";
  }
  return brackets[brackets.length - 1].rate * 100 + "%";
}
function calcGiftTaxFull(p) {
  const { priorGifts = 0, currentGift = 0, isSpouseGift = false, year } = p;
  const v = getVersion2(year);
  if (isSpouseGift) {
    return {
      isSpouseGift: true,
      totalGifts: priorGifts + currentGift,
      annualExempt: v.annualExempt,
      usedExempt: Math.min(priorGifts, v.annualExempt),
      remainingExempt: Math.max(0, v.annualExempt - priorGifts),
      netGift: 0,
      tax: 0,
      rate: "\u514D\u7A05",
      taxYear: year || LATEST_GIFT_YEAR,
      lawVersion: v.lawVersion,
      law: "\u907A\u7522\u53CA\u8D08\u8207\u7A05\u6CD5\xA720-I-6\uFF08\u914D\u5076\u76F8\u4E92\u8D08\u8207\u4E4B\u8CA1\u7522\u4E0D\u8A08\u5165\u8D08\u8207\u7E3D\u984D\uFF0C\u7121\u91D1\u984D\u4E0A\u9650\uFF09"
    };
  }
  const totalGifts = priorGifts + currentGift;
  const netGift = Math.max(0, totalGifts - v.annualExempt);
  const tax = calcGiftTax(netGift, { year });
  return {
    isSpouseGift: false,
    totalGifts,
    annualExempt: v.annualExempt,
    usedExempt: Math.min(totalGifts, v.annualExempt),
    remainingExempt: Math.max(0, v.annualExempt - totalGifts),
    netGift,
    tax,
    rate: tax === 0 ? "\u514D\u7A05" : getGiftTaxRate(netGift, { year }),
    taxYear: year || LATEST_GIFT_YEAR,
    lawVersion: v.lawVersion,
    law: "\u907A\u7522\u53CA\u8D08\u8207\u7A05\u6CD5\xA719, \xA722"
  };
}
function planGiftYears(totalAmount, opts = {}) {
  const v = getVersion2(opts.year);
  const freeYears = Math.floor(totalAmount / v.annualExempt);
  const remainder = totalAmount % v.annualExempt;
  return { freeYears, remainder, annualExempt: v.annualExempt };
}

// ../../packages/core-inheritance/src/inheritance.js
function getHeirTier(state) {
  if (state.children > 0) return 1;
  if (state.parents > 0) return 2;
  if (state.siblings > 0) return 3;
  if (state.grandparents > 0) return 4;
  return 0;
}
function getSpouseShare(tier, bloodSlots) {
  if (tier === 0) {
    return { spouseShare: 1, spouseFrac: "\u5168\u90E8", bloodShareTotal: 0 };
  }
  if (tier === 1) {
    const totalHeirs = bloodSlots + 1;
    return {
      spouseShare: 1 / totalHeirs,
      spouseFrac: `1/${totalHeirs}`,
      bloodShareTotal: bloodSlots / totalHeirs
    };
  }
  if (tier <= 3) {
    return { spouseShare: 0.5, spouseFrac: "1/2", bloodShareTotal: 0.5 };
  }
  return { spouseShare: 2 / 3, spouseFrac: "2/3", bloodShareTotal: 1 / 3 };
}
function getForcedShareRate(tier, who) {
  if (who === "spouse") return 0.5;
  if (tier <= 2) return 0.5;
  return 1 / 3;
}
function calcStatutoryShare(p) {
  const {
    totalEstate,
    hasSpouse,
    hasWill = false,
    children = 0,
    parents = 0,
    siblings = 0,
    grandparents = 0,
    deceasedChildren = 0,
    representatives = [],
    renounce = {}
  } = p;
  const tier = getHeirTier({ children, parents, siblings, grandparents });
  const tierKey = (
    /** @type {''|'children'|'parents'|'siblings'|'grandparents'} */
    ["", "children", "parents", "siblings", "grandparents"][tier]
  );
  const bloodCount = tierKey === "children" ? children : tierKey === "parents" ? parents : tierKey === "siblings" ? siblings : tierKey === "grandparents" ? grandparents : 0;
  const renounced = tierKey && renounce[
    /** @type {'children'|'parents'|'siblings'|'grandparents'} */
    tierKey
  ] || 0;
  const effectiveBlood = Math.max(0, bloodCount - renounced);
  if (tier === 1 && effectiveBlood <= 0) {
    return {
      warning: "\u5168\u6578\u5B50\u5973\u62CB\u68C4\u7E7C\u627F",
      law: "\u6C11\u6CD5\xA71176",
      heirs: []
    };
  }
  let bloodSlots = effectiveBlood;
  if (tier === 1) {
    const dec = deceasedChildren;
    const aliveActive = effectiveBlood - dec;
    bloodSlots = aliveActive + dec;
  }
  const { spouseShare, spouseFrac, bloodShareTotal } = hasSpouse ? getSpouseShare(tier, bloodSlots) : { spouseShare: 0, spouseFrac: "0", bloodShareTotal: 1 };
  const spouseAmount = hasSpouse ? totalEstate * spouseShare : 0;
  const bloodPool = hasSpouse ? totalEstate * bloodShareTotal : totalEstate;
  const heirs = [];
  if (hasSpouse) {
    heirs.push({
      role: "spouse",
      label: "\u914D\u5076",
      fraction: spouseFrac,
      amount: spouseAmount,
      forcedShare: hasWill ? spouseAmount * getForcedShareRate(tier, "spouse") : null
    });
  }
  if (tier === 1 && effectiveBlood > 0) {
    const dec = deceasedChildren;
    const aliveActive = effectiveBlood - dec;
    const totalSlots = aliveActive + dec;
    const perSlot = totalSlots > 0 ? bloodPool / totalSlots : 0;
    for (let i = 0; i < aliveActive; i++) {
      heirs.push({
        role: "child",
        label: `\u5B50\u5973\uFF08\u7B2C${i + 1}\u4EBA\uFF09`,
        amount: perSlot,
        forcedShare: hasWill ? perSlot * getForcedShareRate(tier, "blood") : null
      });
    }
    for (let i = 0; i < dec; i++) {
      const repCount = representatives[i] || 1;
      const perRep = perSlot / repCount;
      for (let j = 0; j < repCount; j++) {
        heirs.push({
          role: "representative",
          label: `\u5B6B\u5B50\u5973\uFF08\u4EE3\u4F4D\u7B2C${i + 1}\u4F4D\u904E\u4E16\u5B50\u5973\uFF0C\u7B2C${j + 1}\u4EBA\uFF09`,
          amount: perRep,
          note: "\u4EE3\u4F4D\u7E7C\u627F",
          forcedShare: hasWill ? perRep * getForcedShareRate(tier, "blood") : null
        });
      }
    }
  } else if (effectiveBlood > 0) {
    const perBlood = bloodPool / effectiveBlood;
    const label = tier === 2 ? "\u7236\u6BCD" : tier === 3 ? "\u5144\u5F1F\u59CA\u59B9" : "\u7956\u7236\u6BCD";
    for (let i = 0; i < effectiveBlood; i++) {
      heirs.push({
        role: label,
        label: label + (effectiveBlood > 1 ? `\uFF08\u7B2C${i + 1}\u4EBA\uFF09` : ""),
        amount: perBlood,
        forcedShare: hasWill ? perBlood * getForcedShareRate(tier, "blood") : null
      });
    }
  }
  return {
    tier,
    heirs,
    renounced,
    law: "\u6C11\u6CD5\xA71138, \xA71144, \xA71140" + (hasWill ? ", \xA71223" : "")
  };
}

// ../../packages/core-inheritance/src/property-tax.js
var PROPERTY_TAX_VERSIONS = {
  2026: {
    lawVersion: "2026-Q1",
    // 房地合一稅 2.0 稅率在可預見期間內穩定。變動時請在此新增年度。
    rates: { long10: 0.15, long5: 0.2, short2: 0.35, short1: 0.45, forced: 0.2 }
  }
};
var LATEST_PROPERTY_YEAR = 2026;
function getPropertyTaxRate(holdYears, isForced = false, opts = {}) {
  const v = PROPERTY_TAX_VERSIONS[
    /** @type {keyof typeof PROPERTY_TAX_VERSIONS} */
    opts.year || LATEST_PROPERTY_YEAR
  ] || PROPERTY_TAX_VERSIONS[LATEST_PROPERTY_YEAR];
  const r = v.rates;
  if (holdYears >= 10) return r.long10;
  if (holdYears >= 5) return r.long5;
  if (isForced) return r.forced;
  if (holdYears >= 2) return r.short2;
  return r.short1;
}
function calcPropertyTaxOnce({ salePrice, acquisitionCost, deductibleExpense, extraBurden, taxRate }) {
  const taxableGain = Math.max(0, salePrice - acquisitionCost - deductibleExpense - extraBurden);
  const tax = Math.round(taxableGain * taxRate);
  return { taxableGain, tax };
}
function calcInheritedPropertyTrap(p) {
  const {
    salePrice,
    mortgage,
    inheritedValue,
    holdYears,
    cpiMult = 1.08,
    expenseRate = 0.03,
    isForced = false,
    year
  } = p;
  const smartRate = getPropertyTaxRate(holdYears, isForced, { year });
  const naiveRate = getPropertyTaxRate(holdYears, false, { year });
  const naiveCost = inheritedValue;
  const naiveExp = 0;
  const naiveExtBurden = 0;
  const naive = calcPropertyTaxOnce({
    salePrice,
    acquisitionCost: naiveCost,
    deductibleExpense: naiveExp,
    extraBurden: naiveExtBurden,
    taxRate: naiveRate
  });
  const smartCost = inheritedValue * cpiMult;
  const smartExp = salePrice * expenseRate;
  const extBurden = Math.max(0, mortgage - inheritedValue);
  const smart = calcPropertyTaxOnce({
    salePrice,
    acquisitionCost: smartCost,
    deductibleExpense: smartExp,
    extraBurden: extBurden,
    taxRate: smartRate
  });
  const savings = naive.tax - smart.tax;
  const savingsPct = naive.tax > 0 ? Math.round(savings / naive.tax * 100) : 0;
  const cpiSaving = Math.round((smartCost - naiveCost) * smartRate);
  const expSaving = Math.round(smartExp * smartRate);
  const extSaving = Math.round(extBurden * smartRate);
  const rateSaving = naiveRate !== smartRate ? Math.round(naive.taxableGain * (naiveRate - smartRate)) : 0;
  const cashOnHand = salePrice - mortgage;
  const isTrap = cashOnHand > 0 && naive.tax > cashOnHand * 0.5;
  const isExtremeTrap = naive.tax >= cashOnHand;
  const v = PROPERTY_TAX_VERSIONS[
    /** @type {keyof typeof PROPERTY_TAX_VERSIONS} */
    year || LATEST_PROPERTY_YEAR
  ] || PROPERTY_TAX_VERSIONS[LATEST_PROPERTY_YEAR];
  return {
    naive: {
      cost: naiveCost,
      expense: naiveExp,
      extraBurden: naiveExtBurden,
      taxableGain: naive.taxableGain,
      tax: naive.tax,
      rate: naiveRate
    },
    smart: {
      cost: smartCost,
      expense: smartExp,
      extraBurden: extBurden,
      taxableGain: smart.taxableGain,
      tax: smart.tax,
      rate: smartRate
    },
    savings,
    savingsPct,
    breakdown: {
      cpiSaving,
      expSaving,
      extSaving,
      rateSaving
    },
    trap: {
      cashOnHand,
      isTrap,
      isExtremeTrap
    },
    taxYear: year || LATEST_PROPERTY_YEAR,
    lawVersion: v.lawVersion,
    law: "\u6240\xA74-4, \xA74-5, \xA714-4\uFF1B\u8CA1\u90E8\u53F0\u8CA1\u7A05\u5B57\u7B2C 11004561660 \u865F\u51FD\uFF1B\u975E\u81EA\u9858\u51FA\u552E 20% \u4F9D\u6240\xA714-4 III \u7B2C 1 \u6B3E\u53CA\u8CA1\u653F\u90E8\u516C\u544A\u300C\u500B\u4EBA\u53CA\u71DF\u5229\u4E8B\u696D\u975E\u81EA\u9858\u4EA4\u6613\u6301\u6709\u671F\u9593\u5728 5 \u5E74\u4EE5\u4E0B\u4E4B\u623F\u5C4B\u3001\u571F\u5730\uFF0C\u5F97\u6309 20% \u7A05\u7387\u8AB2\u5FB5\u623F\u5730\u5408\u4E00\u6240\u5F97\u7A05\u60C5\u5F62\u300D"
  };
}

// ../../packages/core-inheritance/src/utils/dates-tw.js
function parseDateTW(s) {
  if (s == null || s === "") return /* @__PURE__ */ new Date(NaN);
  if (s instanceof Date) return new Date(s.getTime());
  const str = String(s).trim();
  if (/[TZ]|[+-]\d\d:?\d\d$/.test(str)) return new Date(str);
  if (/^\d{4}[-/]\d{1,2}[-/]\d{1,2}$/.test(str)) {
    return /* @__PURE__ */ new Date(str.replace(/\//g, "-") + "T00:00:00+08:00");
  }
  return new Date(str);
}

// ../../packages/core-inheritance/src/damages.js
var SOLATIUM_INJURY = {
  light: { low: 1e5, high: 5e5, mid: 2e5 },
  serious: { low: 5e5, high: 2e6, mid: 1e6 },
  severe: { low: 15e5, high: 3e6, mid: 2e6 }
};
var SOLATIUM_DEATH = {
  low: { low: 1e6, high: 25e5, mid: 15e5 },
  mid: { low: 1e6, high: 25e5, mid: 175e4 },
  high: { low: 1e6, high: 25e5, mid: 2e6 }
};
var COMPULSORY_INJURY_MAX = 2e5;
var COMPULSORY_DEATH_MAX = 2e6;
function calcInjuryDamages(p) {
  const {
    medical = 0,
    careDays = 0,
    careRate = 2400,
    workLossDays = 0,
    monthlySalary = 0,
    solatiumLevel = "light",
    faultPct = 1
  } = p;
  const safeFault = Math.max(0, Math.min(1, faultPct));
  const safeLevel = SOLATIUM_INJURY[solatiumLevel] ? solatiumLevel : "light";
  const careTotal = careDays * careRate;
  const workLoss = monthlySalary / 30 * workLossDays;
  const solatium = SOLATIUM_INJURY[safeLevel].mid;
  const subtotal = medical + careTotal + workLoss + solatium;
  const insuranceOffset = Math.min(COMPULSORY_INJURY_MAX, medical);
  const netAmount = Math.max(0, (subtotal - insuranceOffset) * safeFault);
  return {
    items: {
      medical,
      care: careTotal,
      workLoss,
      solatium
    },
    subtotal,
    insuranceOffset,
    faultPct,
    netAmount,
    law: "\u6C11\u6CD5\xA7184, \xA7193, \xA7195, \xA7197\uFF1B\u5F37\u5236\u6C7D\u8ECA\u8CAC\u4EFB\u4FDD\u96AA\u7D66\u4ED8\u6A19\u6E96\xA72\uFF08\u50B7\u5BB3\u91AB\u7642\u7D66\u4ED8 20 \u842C\uFF09"
  };
}
function calcDeathDamages(p) {
  const {
    funeral = 0,
    dependentCount = 0,
    dependentYears = 0,
    monthlySalary = 0,
    solatiumLevel = "mid",
    faultPct = 1,
    compulsoryInsurance = 0
  } = p;
  const safeFault = Math.max(0, Math.min(1, faultPct));
  const safeLevel = SOLATIUM_DEATH[solatiumLevel] ? solatiumLevel : "mid";
  const maintenance = monthlySalary * 12 * dependentYears * dependentCount;
  const solatium = SOLATIUM_DEATH[safeLevel].mid;
  const subtotal = funeral + maintenance + solatium;
  const faultShare = subtotal * safeFault;
  const insuranceOffset = Math.min(Math.max(0, compulsoryInsurance), COMPULSORY_DEATH_MAX);
  const netAmount = Math.max(0, faultShare - insuranceOffset);
  return {
    items: {
      funeral,
      maintenance,
      solatium
    },
    subtotal,
    faultShare,
    insuranceOffset,
    faultPct,
    netAmount,
    law: "\u6C11\u6CD5\xA7184, \xA7192, \xA7194, \xA7197\uFF1B\u5F37\u5236\u6C7D\u8ECA\u8CAC\u4EFB\u4FDD\u96AA\u6CD5\xA732\u3001\u7D66\u4ED8\u6A19\u6E96\xA76\uFF08\u6B7B\u4EA1\u7D66\u4ED8\u4E0A\u9650 200 \u842C\uFF0C\u91D1\u7BA1\u6703\u9810\u544A\u64EC\u81EA 2026/7/1 \u8ABF\u5347\u81F3 300 \u842C\uFF0C\u5C1A\u672A\u767C\u5E03\u751F\u6548\uFF1B\u5F37\u5236\u96AA\u4EE5\u5BE6\u969B\u9818\u5F97\u7D66\u4ED8\u70BA\u9650\u62B5\u6263\uFF09"
  };
}
function calcDamageDeadline(eventDate, knownDate, today = /* @__PURE__ */ new Date()) {
  const event = parseDateTW(eventDate);
  const known = parseDateTW(knownDate || eventDate);
  const ms = 864e5;
  const deadline2y = new Date(known);
  deadline2y.setFullYear(deadline2y.getFullYear() + 2);
  const deadline10y = new Date(event);
  deadline10y.setFullYear(deadline10y.getFullYear() + 10);
  const hard = deadline2y.getTime() < deadline10y.getTime() ? deadline2y : deadline10y;
  const daysLeft2y = Math.ceil((deadline2y.getTime() - today.getTime()) / ms);
  const daysLeft10y = Math.ceil((deadline10y.getTime() - today.getTime()) / ms);
  return {
    daysLeft2y,
    daysLeft10y,
    hardDeadline: hard,
    expired: today.getTime() > hard.getTime(),
    law: "\u6C11\u6CD5\xA7197"
  };
}

// ../../packages/core-inheritance/src/insurance.js
var INSURANCE_VERSIONS = {
  2026: {
    lawVersion: "2026-Q1",
    amtExemption: 75e5,
    // 個人基本所得額扣除額 750 萬
    amtRate: 0.2,
    // 最低稅負稅率 20%
    lifeInsuranceAmtThreshold: 374e5,
    // 所得基本稅額條例§12-I-2 保險死亡給付免稅額（財政部 112/11/23 台財稅字第 11204674390 號公告，113 年度起自 3,330 萬調為 3,740 萬；114、115 年度沿用）
    // 舊欄位：遺贈稅法§16(9) 本身**無金額上限**，是否計入遺產由「實質課稅原則」個案判斷
    //   （釋字 420、財政部 101.7.13 台財稅字第 10100107900 號函）。
    //   前版常數 33_300_000（舊 AMT 免稅額）被誤述為「遺產稅 免計上限」，已停用；
    //   若工具需要警示門檻請改用 lifeInsuranceAmtThreshold。
    lifeInsuranceEstateExclude: 374e5
    // 向後相容保留；語意同 lifeInsuranceAmtThreshold（非遺產稅法上限）
  }
};
var LATEST_INSURANCE_YEAR = 2026;
function getVersion3(year) {
  const y = year || LATEST_INSURANCE_YEAR;
  return INSURANCE_VERSIONS[
    /** @type {keyof typeof INSURANCE_VERSIONS} */
    y
  ] || INSURANCE_VERSIONS[LATEST_INSURANCE_YEAR];
}
var AMT_EXEMPTION = INSURANCE_VERSIONS[LATEST_INSURANCE_YEAR].amtExemption;
var AMT_RATE = INSURANCE_VERSIONS[LATEST_INSURANCE_YEAR].amtRate;
var LIFE_INSURANCE_AMT_THRESHOLD = INSURANCE_VERSIONS[LATEST_INSURANCE_YEAR].lifeInsuranceAmtThreshold;
var LIFE_INSURANCE_ESTATE_EXCLUDE = INSURANCE_VERSIONS[LATEST_INSURANCE_YEAR].lifeInsuranceEstateExclude;
function calcAmt(p) {
  const { regularIncomeTax = 0, insurancePayout = 0, otherAmtItems = 0, year } = p;
  const v = getVersion3(year);
  const insuranceAmtBase = Math.max(0, insurancePayout - v.lifeInsuranceAmtThreshold);
  const amtBase = insuranceAmtBase + otherAmtItems;
  const netAmtBase = Math.max(0, amtBase - v.amtExemption);
  const amtTax = Math.round(netAmtBase * v.amtRate);
  const amtPayable = Math.max(0, amtTax - regularIncomeTax);
  return {
    insuranceAmtBase,
    amtBase,
    netAmtBase,
    amtTax,
    regularIncomeTax,
    amtPayable,
    law: "\u6240\u5F97\u57FA\u672C\u7A05\u984D\u689D\u4F8B\xA712, \xA713"
  };
}
function analyzeInsuranceEstateRisk(p) {
  const { insuranceAmount = 0, designatedBeneficiary = false, suspiciousFeatures = false, year } = p;
  if (!designatedBeneficiary) {
    return {
      includedInEstate: insuranceAmount,
      excluded: 0,
      reason: "\u672A\u6307\u5B9A\u53D7\u76CA\u4EBA\uFF0C\u5168\u984D\u8A08\u5165\u907A\u7522",
      law: "\u907A\u8D08\u7A05\u6CD5\xA716(9)"
    };
  }
  if (suspiciousFeatures) {
    return {
      includedInEstate: insuranceAmount,
      excluded: 0,
      reason: "\u5177\u5BE6\u8CEA\u8AB2\u7A05\u7279\u5FB5\uFF08\u5982\u8E89\u7E73\u3001\u9AD8\u9F61\u3001\u5E36\u75C5\u6295\u4FDD\u7B49\uFF09\uFF0C\u570B\u7A05\u5C40\u53EF\u80FD\u5168\u984D\u7D0D\u5165\u907A\u7522",
      riskLevel: "high",
      law: "\u907A\u8D08\u7A05\u6CD5\xA716(9) + \u5BE6\u8CEA\u8AB2\u7A05\u539F\u5247"
    };
  }
  return {
    includedInEstate: 0,
    excluded: insuranceAmount,
    reason: "\u6307\u5B9A\u53D7\u76CA\u4EBA\u4E14\u7121\u5BE6\u8CEA\u8AB2\u7A05\u7279\u5FB5\uFF0C\u514D\u8A08\u5165\u907A\u7522",
    riskLevel: "low",
    law: "\u907A\u8D08\u7A05\u6CD5\xA716(9)"
  };
}
function compareLoanVsSurrender(p) {
  const { surrenderValue = 0, loanRate = 2.5, loanYears = 1 } = p;
  const loanableAmount = surrenderValue * 0.7;
  const totalInterest = loanableAmount * (loanRate / 100) * loanYears;
  const netAfterLoan = loanableAmount - totalInterest;
  return {
    surrenderValue,
    loanableAmount,
    totalInterest,
    netAfterLoan,
    // 解約一次取得 surrenderValue；保單借款則保留保單但負利息
    diff: surrenderValue - netAfterLoan
  };
}

// ../../packages/core-inheritance/src/interest.js
var LEGAL_RATE_ANNUAL = 0.05;
var DAYS_PER_YEAR = 365;
function simpleInterest(principal, ratePct, days) {
  const r = ratePct / 100;
  return principal * r * (days / DAYS_PER_YEAR);
}
function compoundInterest(principal, ratePct, days) {
  const r = ratePct / 100;
  const years = days / DAYS_PER_YEAR;
  return principal * (Math.pow(1 + r, years) - 1);
}
function calcInterest(p) {
  const { principal, ratePct = LEGAL_RATE_ANNUAL * 100, days, method = "simple" } = p;
  const interest = method === "compound" ? compoundInterest(principal, ratePct, days) : simpleInterest(principal, ratePct, days);
  return {
    principal,
    ratePct,
    days,
    method,
    interest: Math.round(interest),
    total: Math.round(principal + interest),
    law: "\u6C11\u6CD5\xA7203\uFF08\u6CD5\u5B9A\u9031\u5E74\u5229\u7387 5%\uFF1B\u672C\u6A21\u7D44\u4EE5 365 \u5929/\u5E74 \u7C21\u5316\uFF0C\u8DE8\u958F\u5E74\u91CD\u5927\u91D1\u984D\u5EFA\u8B70\u5BE6\u969B\u65E5\u6578\u6838\u7B97\uFF09"
  };
}

// ../../packages/core-inheritance/src/dates.js
var MS_PER_DAY = 864e5;
function daysBetween(a, b) {
  const d1 = parseDateTW(a), d2 = parseDateTW(b);
  return Math.floor((d2.getTime() - d1.getTime()) / MS_PER_DAY);
}
function yearsBetween(a, b) {
  const d1 = parseDateTW(a), d2 = parseDateTW(b);
  let years = d2.getFullYear() - d1.getFullYear();
  const m = d2.getMonth() - d1.getMonth();
  if (m < 0 || m === 0 && d2.getDate() < d1.getDate()) years--;
  return years;
}
function addYears(date, years) {
  const d = parseDateTW(date);
  d.setFullYear(d.getFullYear() + years);
  return d;
}
function addMonths(date, months) {
  const d = parseDateTW(date);
  d.setMonth(d.getMonth() + months);
  return d;
}
function calcStatuteOfLimitations(fromDate, limitYears, today = /* @__PURE__ */ new Date()) {
  const deadline = addYears(fromDate, limitYears);
  const daysLeft = Math.ceil((deadline.getTime() - today.getTime()) / MS_PER_DAY);
  return {
    deadline,
    daysLeft,
    expired: daysLeft <= 0
  };
}
function calcHoldingYears(acquiredDate, saleDate, deceasedAcquiredDate) {
  const sale = saleDate ? parseDateTW(saleDate) : /* @__PURE__ */ new Date();
  const ownerYears = yearsBetween(acquiredDate, sale);
  if (!deceasedAcquiredDate) return ownerYears;
  const combinedYears = yearsBetween(deceasedAcquiredDate, sale);
  return { ownerYears, combinedYears };
}
function getInheritanceDeadlines(deathDate) {
  const d = parseDateTW(deathDate);
  return {
    renounce: { label: "\u62CB\u68C4\u7E7C\u627F", deadline: addMonths(d, 3), law: "\u6C11\u6CD5\xA71174" },
    limitedAccept: { label: "\u9650\u5B9A\u7E7C\u627F", deadline: addMonths(d, 3), law: "\u6C11\u6CD5\xA71156" },
    estateTaxReport: { label: "\u907A\u7522\u7A05\u7533\u5831", deadline: addMonths(d, 6), law: "\u907A\u7522\u53CA\u8D08\u8207\u7A05\u6CD5\xA723" },
    incomeTaxReport: { label: "\u7D9C\u5408\u6240\u5F97\u7A05", deadline: addMonths(d, 3), law: "\u6240\u5F97\u7A05\u6CD5\xA771-1" }
  };
}
function formatDate(date) {
  return parseDateTW(date).toLocaleDateString("zh-TW", {
    year: "numeric",
    month: "long",
    day: "numeric"
  });
}

// ../../packages/core-inheritance/src/record.js
var SCHEMA_VERSION = 1;
var DRAFT_TTL_MS = 72 * 60 * 60 * 1e3;
function _deepClone(v) {
  if (typeof structuredClone === "function") return structuredClone(v);
  return v == null ? v : JSON.parse(JSON.stringify(v));
}
function createCalcRecord({
  toolId,
  persona = "public",
  input,
  result,
  taxYear,
  lawVersion,
  meta
}) {
  return {
    schemaVersion: SCHEMA_VERSION,
    toolId,
    persona,
    input: _deepClone(input),
    result: _deepClone(result),
    taxYear: taxYear ?? (/* @__PURE__ */ new Date()).getFullYear(),
    lawVersion: lawVersion || `${(/* @__PURE__ */ new Date()).getFullYear()}-Q1`,
    meta: meta || null,
    createdAt: (/* @__PURE__ */ new Date()).toISOString()
  };
}
function loadDraft(key) {
  try {
    const raw = localStorage.getItem(`ip_draft_${key}`);
    if (!raw) return null;
    const wrapped = JSON.parse(raw);
    if (wrapped && typeof wrapped === "object" && wrapped.__ts) {
      if (Date.now() - wrapped.__ts > DRAFT_TTL_MS) {
        localStorage.removeItem(`ip_draft_${key}`);
        return null;
      }
      return wrapped.data;
    }
    localStorage.removeItem(`ip_draft_${key}`);
    return null;
  } catch (_e) {
    return null;
  }
}
function saveDraft(key, record) {
  try {
    localStorage.setItem(`ip_draft_${key}`, JSON.stringify({
      __ts: Date.now(),
      data: record
    }));
    return true;
  } catch (_e) {
    return false;
  }
}
function clearAllDrafts() {
  let n = 0;
  try {
    const keys = [];
    for (let i = 0; i < localStorage.length; i++) {
      const k = localStorage.key(i);
      if (k && k.startsWith("ip_draft_")) keys.push(k);
    }
    for (const k of keys) {
      localStorage.removeItem(k);
      n++;
    }
  } catch (_e) {
  }
  return n;
}
function attachCaseMeta(record, meta) {
  return { ...record, meta: { ...record.meta, ...meta } };
}

// ../../packages/core-inheritance/src/utils/format.js
var FMT = (v) => "NT$ " + Math.round(Number(v) || 0).toLocaleString("zh-TW");
var FMT_WAN = (v) => Math.round((Number(v) || 0) / 1e4).toLocaleString("zh-TW") + " \u842C";
var FMT_NUM = (v) => Math.round(Number(v) || 0).toLocaleString("zh-TW");
var FMT_PCT = (v, digits = 0) => (Number(v) * 100).toFixed(digits) + "%";
function parseNum(value, fallback = 0) {
  const v = parseFloat(value);
  return isNaN(v) ? fallback : v;
}
function getInputNum(id, fallback = 0) {
  const el = (
    /** @type {HTMLInputElement|null} */
    document.getElementById(id)
  );
  if (!el) return fallback;
  return parseNum(el.value, fallback);
}
export {
  AMT_EXEMPTION,
  AMT_RATE,
  COMPULSORY_DEATH_MAX,
  COMPULSORY_INJURY_MAX,
  DAYS_PER_YEAR,
  DEDUCT_FUNERAL,
  DEDUCT_PER_CHILD,
  DEDUCT_PER_DISABLED,
  DEDUCT_PER_PARENT,
  DEDUCT_SPOUSE,
  DRAFT_TTL_MS,
  ESTATE_EXEMPTION,
  ESTATE_TAX_VERSIONS,
  FMT,
  FMT_NUM,
  FMT_PCT,
  FMT_WAN,
  GIFT_ANNUAL_EXEMPT,
  GIFT_TAX_VERSIONS,
  INSURANCE_EXCLUDE_MAX,
  INSURANCE_VERSIONS,
  LATEST_GIFT_YEAR,
  LATEST_INSURANCE_YEAR,
  LATEST_PROPERTY_YEAR,
  LATEST_TAX_YEAR,
  LEGAL_RATE_ANNUAL,
  LIFE_INSURANCE_AMT_THRESHOLD,
  LIFE_INSURANCE_ESTATE_EXCLUDE,
  PROPERTY_TAX_VERSIONS,
  SCHEMA_VERSION,
  SOLATIUM_DEATH,
  SOLATIUM_INJURY,
  addMonths,
  addYears,
  analyzeInsuranceEstateRisk,
  attachCaseMeta,
  calcAmt,
  calcDamageDeadline,
  calcDeathDamages,
  calcEstateTax,
  calcEstateTaxFull,
  calcGiftTax,
  calcGiftTaxFull,
  calcHoldingYears,
  calcInheritedPropertyTrap,
  calcInjuryDamages,
  calcInterest,
  calcPropertyTaxOnce,
  calcStatuteOfLimitations,
  calcStatutoryShare,
  clearAllDrafts,
  compareLoanVsSurrender,
  compoundInterest,
  createCalcRecord,
  daysBetween,
  formatDate,
  getEstateTaxBreakdown,
  getEstateTaxRate,
  getForcedShareRate,
  getGiftTaxRate,
  getHeirTier,
  getInheritanceDeadlines,
  getInputNum,
  getPropertyTaxRate,
  getSpouseShare,
  loadDraft,
  parseDateTW,
  parseNum,
  planGiftYears,
  saveDraft,
  simpleInterest,
  yearsBetween
};
//# sourceMappingURL=core-inheritance.bundle.js.map
